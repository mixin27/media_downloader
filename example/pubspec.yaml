name: media_downloader_example
description: "Example app for media_downloader package"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  media_downloader:
    path: ../

  # flutter_downloader: ^1.12.0
  # path_provider: ^2.1.5
  # path: ^1.9.1
  # permission_handler: ^12.0.1
  # device_info_plus: ^12.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
